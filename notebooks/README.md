# Notebooks

This folder is intentionally lightweight. The production code lives in `src/`; notebooks should be used only for
exploration, stakeholder storytelling, and model diagnostics that later graduate into tested modules.
